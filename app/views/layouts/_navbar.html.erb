<nav class="navbar sticky-top navbar-expand-lg navbar-light bg-white border-bottom">
  <div class="container-fluid ms-4">
    <a class="navbar-brand mb-0 h1" style="color: #1877f2" href="/">metabook</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Home", "/", class: "nav-link active" %>
        </li>
        <% if current_user %>
          <li>
            <%= link_to "My Profile", "/users/#{current_user.id}", class: "nav-link active" %>
          </li>
        <% end %>
      </ul>
      <div class="col-md-3 text-end">
      <% if current_user %> 
        <span class="rainbow rainbow_text_animated">Welcome <%= current_user.first_name %>!</span>
        <%= link_to "Log out", current_user_session, method: :delete, class: "btn btn-outline-primary me-2" %>
      <% else %>
        <%= link_to "Login", new_user_session_path, class: "btn btn-outline-primary me-2" %>
        <%= link_to "Sign-up", new_user_path, class: "btn btn-primary" %>
      <% end %>
      </div>
    </div>
  </div>
</nav>
